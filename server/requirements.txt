# Зависимости музыкального сервера.
#
# Ставятся поверх того, что уже стоит в контейнере под телеграм-бота
# (aiogram, aiohttp, aiosqlite, python-dotenv). Здесь только то, чего там нет.
#
# Пакета `yt-dlp` тут намеренно нет. Измерено 2026-08-27: версия с PyPI на всех
# пяти клиентах лестницы отдаёт ссылку, которую googlevideo встречает 403 на
# `Range: bytes=0-`; тот же запрос ночным **бинарником** отдаёт 206 и читаемое
# аудио. Починки YouTube выходят в ночных сборках в день поломки, а до PyPI идут
# недели. Бинарник скачивает и обновляет сам сервер — см. `ensure_ytdlp_binary`.
#
# aiohttp уже стоит ради вебапки бота; фиксируем нижнюю границу, чтобы
# `WebSocketResponse(protocols=...)` был на месте, и верхнюю — контейнер
# переустанавливает зависимости на каждом запуске.
aiohttp>=3.9.0,<4.0.0
