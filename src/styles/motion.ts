/**
 * Длительности движения, которые нужны и разметке, и коду.
 *
 * Единственное место, где число из таблицы стилей продублировано в JS, и это
 * вынужденно: анимация ухода живёт в CSS, а снять элемент из дерева должен
 * React — значит он обязан знать, сколько кадры играют.
 *
 * Почему не `animationend`. Событие не приходит, если движение отключено
 * системной настройкой «меньше движения» — а в этом режиме мы как раз и
 * схлопываем длительности до миллисекунды. Уведомление или окно осталось бы
 * висеть навсегда именно у того, кто анимаций не просил.
 *
 * Согласованность с `--dur-fast` проверяется тестом (tests/unit/motion.test.ts),
 * так что разъехаться незаметно эти два значения не могут.
 */
export const EXIT_MS = 150;
